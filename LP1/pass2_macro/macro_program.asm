START 100
MACRO
INCR_REG &REG, &VAL=1
    ADD &REG, &VAL
    MOVER AREG, &REG
MEND
MACRO
SWAP &X, &Y
    MOVER AREG, &X
    MOVEM AREG, &Y
    MOVER AREG, &Y
    MOVEM AREG, &X
MEND
INCR_REG BREG, 5
SWAP R1, R2
INCR_REG CREG
END